import Registation from '@/components/Registation';
import React from 'react';

export const metadata = {
    title: 'Registation',
};

function page() {
    return <Registation />;
}

export default page;
