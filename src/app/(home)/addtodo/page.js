import AddUpdate from '@/components/Add';
import React from 'react';

export const metadata = {
    title: 'Add',
};

function page() {
    return (
        <div>
            <AddUpdate />
        </div>
    );
}

export default page;
