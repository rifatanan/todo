import LogIn from '@/components/LogIn';
import React from 'react';

export const metadata = {
    title: 'LogIn',
};

function page() {
    return (
        <div>
            <LogIn />
        </div>
    );
}

export default page;
