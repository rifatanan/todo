import Edit from '@/components/EditUpdate';
import React from 'react';

export const metadata = {
    title: 'Edit & Update',
};

function page() {
    return (
        <div>
            <Edit />
        </div>
    );
}

export default page;
